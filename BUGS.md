When completely irrelevant questions are given to the chatbot, the bot correctly responds by saying it doesn't have the proper sources, however it still lists sources.
Bot does not wait for response to finish generation before allowing the user to send another request. This is also true for the shortcut buttons, as the user is able to spam click one or multiple buttons in seconds although only the first request garners a response. Add a timeout where the user has to wait for the response to finish generating before being able to give another request OR manually stop by pressing the stop button (which should replace the enter button when a message is generating).
